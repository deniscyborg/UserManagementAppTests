name: .NET CI - Allure

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      # ОСТАВЬ ТУ, ЧТО НУЖНА ТЕБЕ В ТЕСТОВОМ ПРОЕКТЕ (РЕКОМЕНДУЮ .NET 6)
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '6.0.x'

      # Принудительно устанавливаем требуемую версию Allure.NUnit (например, 2.13.8, если она доступна)
      - name: Install exact Allure.NUnit version
        run: dotnet add tests/tests.csproj package Allure.NUnit --version 2.13.8

      # Дальше restore/build/run как обычно
      - name: Restore dependencies
        run: dotnet restore tests/tests.csproj

      - name: Build tests
        run: dotnet build tests/tests.csproj --no-restore

      - name: Run tests and generate Allure results
        env:
          ALLURE_RESULTS_DIRECTORY: allure-results
        run: dotnet test tests/tests.csproj --no-build --verbosity normal

      - name: Upload Allure results as artifact
        uses: actions/upload-artifact@v4
        with:
          name: allure-results
          path: allure-results

      - name: Install Allure CLI
        run: |
          wget https://github.com/allure-framework/allure2/releases/download/2.27.0/allure-2.27.0.zip
          unzip allure-2.27.0.zip -d /opt/
          ln -s /opt/allure-2.27.0/bin/allure /usr/bin/allure

      - name: Generate Allure HTML report
        run: allure generate allure-results -o allure-report

      - name: Upload Allure HTML report
        uses: actions/upload-artifact@v4
        with:
          name: allure-report
          path: allure-report
