name: .NET CI - Allure

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup .NET SDK for tests
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '6.0.x'

      - name: Setup .NET SDK for backend
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Start backend
        run: |
          cd backend
          dotnet restore
          dotnet build --no-restore
          nohup dotnet run --no-build --urls "http://localhost:5000" > backend.log 2>&1 &
          sleep 10
          echo "Backend started on port 5000"

      - name: Start frontend
        run: |
          cd frontend
          npm install
          nohup npm run dev > frontend.log 2>&1 &
          sleep 15
          echo "Frontend started on port 5173"

      - name: Verify services are running
        run: |
          curl -f http://localhost:5173 || echo "Frontend not ready"
          echo "Services verification complete"

      - name: Restore test dependencies
        run: dotnet restore tests/tests.csproj

      - name: Build tests
        run: dotnet build tests/tests.csproj --no-restore
      
      - name: List tests
        run: dotnet test tests/tests.csproj --list-tests --no-build

      - name: Install Playwright browsers
        run: pwsh tests/bin/Debug/net6.0/playwright.ps1 install --with-deps

      - name: Run tests and generate Allure results
        env:
          BASE_URL: "http://localhost:5173"
          API_BASE_URL: "http://localhost:5000/api"
          BROWSER: "chromium"
        run: dotnet test tests/tests.csproj --no-build --verbosity normal
        continue-on-error: true

      - name: Show backend logs
        if: always()
        run: cat backend/backend.log || echo "No backend logs"

      - name: Show frontend logs
        if: always()
        run: cat frontend/frontend.log || echo "No frontend logs"

      - name: Upload Allure results as artifact
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: allure-results
          path: tests/bin/Debug/net6.0/allure-results

      - name: Install Allure CLI
        if: always()
        run: |
          ALLURE_VERSION=2.27.0
          wget https://github.com/allure-framework/allure2/releases/download/${ALLURE_VERSION}/allure-${ALLURE_VERSION}.zip
          unzip allure-${ALLURE_VERSION}.zip -d /opt/
          sudo ln -s /opt/allure-${ALLURE_VERSION}/bin/allure /usr/bin/allure

      - name: Generate Allure HTML report
        if: always()
        run: allure generate tests/bin/Debug/net6.0/
